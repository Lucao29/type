var tipoVar = "primeira declaração";
var condicional = 4;

if(condicional > 3) {
    var tipoVar = "declaração";
tipoVar = "declaração2";
}

console.log('tipoVar:' + tipoVar);

let TipoLet = "primeira declaração";
let condicional = 4;

if(condicional > 3) {
    let TipoLet = "declaração";
TipoLet = "declaração2";
}

console.log('TipoLet' = TipoLet);

const Tipoconst = "primera declaração tipo const";
const condicional = 4;

if(condicional > 3) {
    const Tipoconst = "segunda declaração";
}
//criar um evento on click em uma pagina//
const alerta = () => {
    let variavellocal = "texto";
    alert(variaellocal);
}